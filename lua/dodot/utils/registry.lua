-- Registry system for dodot
-- Handles extensible registration of triggers, actions, and power-ups
-- This will be fully implemented in Phase 1.4

local M = {}

-- Create a new registry instance
function M.new()
    -- Will implement registry creation in Phase 1.4
    local registry = {
        items = {},
        -- API methods will be added here
    }
    return registry
end

-- Registry API methods (placeholders for Phase 1.4)
-- registry.add(key, value)
-- registry.get(key)
-- registry.remove(key)
-- registry.list()

return M

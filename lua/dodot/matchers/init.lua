-- Matchers module initialization
-- Registers all built-in matchers with the registry system

local M = {}

-- Register all built-in matchers
function M.register_matchers(registry)
    -- Will implement matcher registration
    -- Phase 3.2 deliverable will add matcher system
    -- Matchers connect triggers to power-ups
end

return M
